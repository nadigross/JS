/**
 * Todo: add dedup: we actually need to remember chunks already written, and not add them to the xorb, and also
 *  take that into account for file reconstruction
 * Todo: byte grouping?
 */
import { type XetWriteTokenParams } from "./xetWriteToken";
interface XorbEvent {
    event: "xorb";
    xorb: Uint8Array;
    hash: string;
    id: number;
    chunks: Array<{
        hash: string;
        length: number;
    }>;
    files: Array<{
        path: string;
        progress: number;
    }>;
}
export declare function createXorbs(fileSources: AsyncGenerator<{
    content: Blob;
    path: string;
    sha256: string;
}>, params: XetWriteTokenParams): AsyncGenerator<XorbEvent | {
    event: "file";
    path: string;
    hash: string;
    sha256: string;
    /** Percentage of file bytes that were deduplicated (0-1) */
    dedupRatio: number;
    representation: Array<{
        xorbId: number | string;
        indexStart: number;
        indexEnd: number;
        /** Unpacked length */
        length: number;
        rangeHash: string;
    }>;
}, void, undefined>;
export {};
//# sourceMappingURL=createXorbs.d.ts.map